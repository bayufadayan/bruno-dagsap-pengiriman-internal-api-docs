info:
  name: search-shipments
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_BASE_URL}}/shipments/search?q=SHP"
  params:
    - name: q
      value: SHP
      type: query
  auth:
    type: bearer
    token: "{{TOKEN_SUPER_ADMIN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
