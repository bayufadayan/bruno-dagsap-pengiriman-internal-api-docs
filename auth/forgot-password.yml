info:
  name: forgot-password
  type: http
  seq: 5

http:
  method: POST
  url: "{{BACKEND_BASE_URL}}/auth/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": "pic.tiga@spid.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
