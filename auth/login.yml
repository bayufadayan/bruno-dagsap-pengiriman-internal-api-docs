info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{BACKEND_BASE_URL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "identifier": "DUMMY-TEST01",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
