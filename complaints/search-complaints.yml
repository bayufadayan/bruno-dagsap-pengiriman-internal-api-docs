info:
  name: search-complaints
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_BASE_URL}}/complaints/search?q=BOX"
  params:
    - name: q
      value: BOX
      type: query
  auth:
    type: bearer
    token: "{{TOKEN_SUPER_ADMIN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
