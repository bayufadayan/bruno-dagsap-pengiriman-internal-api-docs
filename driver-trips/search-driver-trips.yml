info:
  name: search-driver-trips
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_BASE_URL}}/driver-trips/search?q=DRV-TEST-1"
  params:
    - name: q
      value: DRV-TEST-1
      type: query
  auth:
    type: bearer
    token: "{{TOKEN_SUPER_ADMIN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
