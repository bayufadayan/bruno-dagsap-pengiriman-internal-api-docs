info:
  name: delete-by-id-driver-trip
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BACKEND_BASE_URL}}/driver-trips/:box_id"
  params:
    - name: box_id
      value: 8fa5d209-7603-4625-9cf2-f7b2c01e4a32
      type: path
  auth:
    type: bearer
    token: "{{TOKEN_SUPER_ADMIN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
